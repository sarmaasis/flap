/**
 * Path-only registries for App routing.
 * Full tool/guide metadata lives in marketing.ts / guides.ts for lazy pages + prerender.
 */
export { DNS_TOOL_PATHS, GUIDE_PATHS } from "../../shared/client-routes";
